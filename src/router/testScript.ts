import User from './../models/User'
import Major from './../models/Major'
import Course from './../models/Course'
import Requirement from './../models/Requirement'
import Template from './../models/Template'
import { DEV_MAJOR_CS_ID, ENGI_MATH_1910, ENGI_FWS, TEMPLATE_CS } from './../util/data'

export default async () => {
  // await new Requirement(ENGI_MATH_1910).save()
  // await Template.collection.drop()
  // await new Template(TEMPLATE_CS).save()
  // const doc = await Major.findOne({ name: 'ORE' })
  // doc.slug = 'ORE'
  // doc.name = 'Operations Research'
  // const res = await doc.save()
  // console.log('res', res)
}
